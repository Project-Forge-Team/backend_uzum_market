# Прямые зависимости приложения (транзитивные тянет pip).
# Файл в UTF-8 / LF.
#
# Python: >=3.12 (см. .python-version). Локально в песочнице — 3.11 + Django 5.2,
# на сервере/CI (3.12+) ставится свежий Django 6.x. Код не использует
# версии-специфичные API, поэтому диапазон: >=5.2,<7.

Django>=5.2,<7
djangorestframework==3.18.0
django-cors-headers==4.9.0
drf-spectacular==0.30.0
dj-database-url==3.1.2
whitenoise==6.12.0
python-dotenv==1.2.3
argon2-cffi==25.1.0
gunicorn==26.1.0

# Миниатюры/размеры загруженных картинок (width/height в media_files).
pillow==12.3.0

# Драйвер Postgres: Django 6.x рекомендует psycopg 3.
psycopg[binary]==3.3.4

# Нужен только при заданном REDIS_URL (общие счётчики троттлинга между worker'ами).
redis==8.1.0
