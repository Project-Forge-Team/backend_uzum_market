# Инструменты разработки/CI. Ставить поверх основных:
#   pip install -r requirements/requirements.txt -r requirements/requirements-dev.txt
-r requirements.txt

ruff==0.16.5

# Для scripts/e2e.py — сквозная проверка контракта после деплоя.
requests==2.32.5
