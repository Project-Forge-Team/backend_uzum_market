# Опционально: хранение загрузок (Product.image) в S3-совместимом хранилище.
#
# Зачем: каталог /media на Render эфемерный — всё, что загрузили пользователи,
# стирается при следующем деплое. Включается в settings.py парой
# USE_S3=True + AWS_STORAGE_BUCKET_NAME (см. .env.example).
django-storages==1.14.6
boto3==1.43.82
